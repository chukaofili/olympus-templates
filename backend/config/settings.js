const package = require('../package.json');

module.exports.settings = {
    api: {
        title: 'Sample Backend Template',
        name: 'sample-backend-api',
        version: package.version
    }
}